# ATDD Checklist - Epic 2, Story 2.1: AI Preference Learning Foundation

**Date:** 2026-01-09
**Author:** Wavister
**Primary Test Level:** Integration (Redux/Middleware)

---

## Story Summary

As a fitness user interacting with AI recommendations, I want the system to learn and remember my workout preferences over time, so that I receive increasingly personalized coaching that matches my unique style and goals.

**As a** fitness user
**I want** automatic preference learning
**So that** my coaching experience becomes more personal over time

---

## Acceptance Criteria

1. Given a user has completed multiple workout sessions
2. When they consistently show preferences for certain exercises or intensities
3. Then the AI begins incorporating these preferences into future recommendations
4. And preference learning happens automatically without explicit user input

---

## Failing Tests Created (RED Phase)

### Integration Tests (1 test)

**File:** `src/features/preference-learning/__tests__/PreferenceLearningAutomation.atdd.test.ts` (55 lines)

- ✅ **Test:** should automatically trigger preference learning when a session is deactivated @p0
  - **Status:** RED - AssertionError: expected "vi.fn()" to be called at least once
  - **Verifies:** Automatic trigger of preference learning upon session completion (deactivation)

---

## Required data-testid Attributes

### (None required for this backend-heavy story)

---

## Implementation Checklist

### Test: should automatically trigger preference learning when a session is deactivated @p0

**File:** `src/store/middleware/preferenceLearningMiddleware.ts` (New File) or `src/features/session/store/liveSessionSlice.ts`

**Tasks to make this test pass:**

- [ ] Create a Redux middleware that listens for `liveSession/setIsActive` with `payload: false`
- [ ] In the middleware, when session ends, gather current session data and user feedback
- [ ] Call `PreferenceLearningIntegrationService.updatePreferencesFromSession` with the gathered data
- [ ] Register the new middleware in `src/store/index.ts`
- [ ] OR: Convert `setIsActive` to an async thunk that handles the service call after state update
- [ ] Run test: `npx vitest run src/features/preference-learning/__tests__/PreferenceLearningAutomation.atdd.test.ts`
- [ ] ✅ Test passes (green phase)

**Estimated Effort:** 3 hours

---

## Running Tests

```bash
# Run failing tests
npx vitest run src/features/preference-learning/__tests__/PreferenceLearningAutomation.atdd.test.ts
```

---

**Generated by BMad TEA Agent** - 2026-01-09
