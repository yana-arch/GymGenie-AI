# ATDD Checklist - Epic 9, Story 9.1: Injury History Safety Validation (Safety Record)

**Date:** 2026-01-09
**Author:** Wavister
**Primary Test Level:** Unit/API

---

## Story Summary

As a fitness user with past injuries, I want AI recommendations to always respect my documented injury history, so that I can trust the AI won't suggest exercises that could cause harm.

**As a** fitness user
**I want** a permanent safety record of my injuries
**So that** AI never forgets past risks even if I'm currently feeling better

---

## Acceptance Criteria

1. Given a user has injury history documented in their profile
2. When the AI generates workout recommendations
3. Then all recommendations are automatically filtered to avoid contraindicated exercises
4. And the system maintains a permanent safety record of injury considerations

---

## Failing Tests Created (RED Phase)

### API/Unit Tests (2 tests)

**File:** `src/features/injury-aware/__tests__/InjurySafetyRecord.atdd.test.ts` (75 lines)

- ✅ **Test:** should maintain a history of all reported injuries even if they are currently "recovered" @p0
  - **Status:** RED - TypeError: service.getPermanentSafetyRecord is not a function
  - **Verifies:** Persistence of historical injury data beyond current session state

- ✅ **Test:** should flag recommendations that conflict with "recovered" but significant history @p1
  - **Status:** RED - TypeError: service.filterAgainstHistory is not a function
  - **Verifies:** AI "memory" of past injuries when generating new recommendations

---

## Required data-testid Attributes

### Safety Profile

- `permanent-safety-record-list` - Display of all historical injuries
- `safety-warning-recovered-injury` - Warning badge for exercises risky due to past history

---

## Implementation Checklist

### Test: should maintain a history of all reported injuries even if they are currently "recovered" @p0

**File:** `src/features/injury-aware/services/InjuryValidationService.ts`

**Tasks to make this test pass:**

- [ ] Implement `getPermanentSafetyRecord()` method
- [ ] Ensure `storeInjuryHistory` appends to a permanent collection rather than overwriting
- [ ] Implement local storage migration/sync for historical data
- [ ] Run test: `npx vitest run src/features/injury-aware/__tests__/InjurySafetyRecord.atdd.test.ts`
- [ ] ✅ Test passes (green phase)

**Estimated Effort:** 2 hours

---

### Test: should flag recommendations that conflict with "recovered" but significant history @p1

**File:** `src/features/injury-aware/services/InjuryValidationService.ts`

**Tasks to make this test pass:**

- [ ] Implement `filterAgainstHistory(recommendations)` method
- [ ] Add logic to check both active constraints and historical restrictions
- [ ] Define "Dormant but relevant" rules (e.g. past back surgery always flags heavy deadlifts)
- [ ] Run test: `npx vitest run src/features/injury-aware/__tests__/InjurySafetyRecord.atdd.test.ts`
- [ ] ✅ Test passes (green phase)

**Estimated Effort:** 3 hours

---

## Running Tests

```bash
# Run failing tests
npx vitest run src/features/injury-aware/__tests__/InjurySafetyRecord.atdd.test.ts
```

---

**Generated by BMad TEA Agent** - 2026-01-09
