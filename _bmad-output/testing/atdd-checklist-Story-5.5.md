# ATDD Checklist - Epic 5, Story 5.5: Progress Predictions

**Date:** 2026-01-09
**Author:** Wavister
**Primary Test Level:** Unit/API

---

## Story Summary

As a fitness user planning my fitness future, I want AI-powered predictions of my progress based on current performance, so that I can set realistic goals and stay motivated by seeing potential outcomes.

**As a** fitness user
**I want** AI-powered progress predictions
**So that** I can see realistic future results based on my current trajectory

---

## Acceptance Criteria

1. Given a user has consistent workout data
2. When they request progress predictions
3. Then they receive realistic projections based on their current trajectory
4. And predictions include confidence levels and factors that could influence outcomes

---

## Failing Tests Created (RED Phase)

### API/Unit Tests (2 tests)

**File:** `src/features/analytics/__tests__/ProgressPredictions.atdd.test.ts` (60 lines)

- ✅ **Test:** should provide confidence scores and influence factors for each prediction @p0
  - **Status:** RED - AssertionError: expected undefined to be defined
  - **Verifies:** Granular confidence scores and qualitative factors (e.g. volume, consistency)

- ✅ **Test:** should detect non-linear trajectories for more realistic projections @p1
  - **Status:** RED - AssertionError: expected 40.47... to be greater than 60
  - **Verifies:** Automatic detection of exponential vs linear trends

---

## Required data-testid Attributes

### Progress Forecast Chart

- `forecast-confidence-badge` - Numeric display of prediction confidence
- `influence-factor-list` - List of factors affecting the forecast
- `forecast-model-indicator` - Label showing which model (linear/exp) was used

---

## Implementation Checklist

### Test: should provide confidence scores and influence factors for each prediction @p0

**File:** `src/features/analytics/services/PredictionService.ts`

**Tasks to make this test pass:**

- [ ] Update `PredictionResult` interface to include `confidenceScore: number` and `influenceFactors: { factor: string, impact: 'positive' | 'negative' }[]`
- [ ] Implement `calculateConfidenceScore` logic based on data density and variance
- [ ] Implement `identifyInfluenceFactors` logic (e.g. check if volume has been increasing recently)
- [ ] Run test: `npx vitest run src/features/analytics/__tests__/ProgressPredictions.atdd.test.ts`
- [ ] ✅ Test passes (green phase)

**Estimated Effort:** 3 hours

---

### Test: should detect non-linear trajectories for more realistic projections @p1

**File:** `src/features/analytics/services/PredictionService.ts`

**Tasks to make this test pass:**

- [ ] Update `predictFuturePerformance` to support `'auto'` as a model parameter
- [ ] Implement "Best fit" detection (compare R-squared values of linear vs log-linear/exponential fits)
- [ ] Automatically select the model with the better fit
- [ ] Run test: `npx vitest run src/features/analytics/__tests__/ProgressPredictions.atdd.test.ts`
- [ ] ✅ Test passes (green phase)

**Estimated Effort:** 4 hours

---

## Running Tests

```bash
# Run failing tests
npx vitest run src/features/analytics/__tests__/ProgressPredictions.atdd.test.ts
```

---

**Generated by BMad TEA Agent** - 2026-01-09
