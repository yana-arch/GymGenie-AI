# ATDD Checklist - Epic 3, Story 3.1: Local Data Processing Setup

**Date:** 2026-01-09
**Author:** Wavister
**Primary Test Level:** API/E2E

---

## Story Summary

As a privacy-conscious fitness user, I want all my health and workout data processed locally on my device, so that I have complete control over my personal information without cloud dependency.

**As a** privacy-conscious user
**I want** local-only processing
**So that** my data never leaves my device unauthorized

---

## Acceptance Criteria

1. Given a user has health data on their device
2. When they use AI coaching features
3. Then all processing happens locally without data transmission
4. And no sensitive data leaves the device during normal operation

---

## Failing Tests Created (RED Phase)

### API/E2E Tests (2 tests)

**File:** `src/features/privacy/__tests__/PrivacyShieldService.atdd.test.ts` (55 lines)

- ✅ **Test:** should maintain an audit trail of data access by AI service @p0
  - **Status:** RED - TypeError: privacyShieldService.logDataAccess is not a function
  - **Verifies:** Transparency and auditing of data usage (Story 3.3 cross-over)

- ✅ **Test:** should block all network traffic during sensitive local AI processing @p0
  - **Status:** RED - TypeError: privacyShieldService.enableNetworkIsolation is not a function
  - **Verifies:** Hard isolation during sensitive inference

---

## Required data-testid Attributes

### Privacy Settings

- `privacy-audit-log` - Container for viewing data access history
- `network-isolation-status` - Indicator showing when isolation is active

---

## Implementation Checklist

### Test: should maintain an audit trail of data access by AI service @p0

**File:** `src/features/privacy/services/PrivacyShieldService.ts`

**Tasks to make this test pass:**

- [ ] Implement `logDataAccess(accessor, fields, purpose)` method
- [ ] Add persistent audit log storage in local DB
- [ ] Implement `getAuditLogs()` for the UI to consume
- [ ] Run test: `npx vitest run src/features/privacy/__tests__/PrivacyShieldService.atdd.test.ts`
- [ ] ✅ Test passes (green phase)

**Estimated Effort:** 2 hours

---

### Test: should block all network traffic during sensitive local AI processing @p0

**File:** `src/features/privacy/services/PrivacyShieldService.ts`

**Tasks to make this test pass:**

- [ ] Implement `enableNetworkIsolation()` using a global fetch interceptor or Proxy
- [ ] Ensure all outgoing requests are queued or blocked during isolation
- [ ] Implement `disableNetworkIsolation()` to restore connectivity
- [ ] Run test: `npx vitest run src/features/privacy/__tests__/PrivacyShieldService.atdd.test.ts`
- [ ] ✅ Test passes (green phase)

**Estimated Effort:** 4 hours

---

## Running Tests

```bash
# Run failing tests
npx vitest run src/features/privacy/__tests__/PrivacyShieldService.atdd.test.ts
```

---

**Generated by BMad TEA Agent** - 2026-01-09
